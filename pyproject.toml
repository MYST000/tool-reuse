[project]
name = "tool-reuse"
version = "0.1.0"
description = "Exact and semantic reuse for OpenHands tool calls"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["tool_reuse*"]

